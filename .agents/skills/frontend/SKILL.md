# Frontend Engineering

Build accessible, responsive interfaces with semantic HTML, clear loading and error states, and locally consistent components. Verify keyboard navigation, contrast, and small-screen behavior. Keep brand- and product-specific visual rules in overrides.
