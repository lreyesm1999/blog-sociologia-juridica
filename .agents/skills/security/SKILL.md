# Security

Treat all external input as untrusted. Do not expose secrets, credentials, private keys, or token values in code, output, fixtures, logs, or documentation. Validate paths before filesystem operations and require explicit authorization before running skill-provided scripts.
