# Testing

Translate acceptance criteria into focused automated tests. Cover successful behavior, boundary conditions, and failures. Run the smallest relevant test suite first, then broader checks; never claim a change works without recording what ran.
